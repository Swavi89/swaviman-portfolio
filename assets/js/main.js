// Typing Animation using Typed JS
var typed = new Typed(".type", {
    strings: ["a Web", "an Backend", "a FullStack"],
    smartBackspace: true,
    startDelay: 1000,
    typeSpeed: 130,
    backDelay: 1000,
    backSpeed: 60,
    loop: true,
  });